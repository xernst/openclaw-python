# Checkpoint — Chapter 5

1. What type does `input()` return?
2. Why does `int(input("..."))` wrap `input()` inside `int()`?
3. Why is `bool("False")` equal to True?
4. Name four falsy values besides `False`.
5. Why does `int("3.9")` raise? What about `int(3.9)`?
6. How do you read two integers separated by a space in one line?
7. What's wrong with `eval(input(...))`?
