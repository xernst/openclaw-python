"""
Exercise 11.5 — Dispatch table.
Build a dict of name->function for 4 operations: add, sub, mul, div.
Each takes (a, b) and returns a number.
Then, given ops=["add","mul","sub","div"], a=10, b=3, print f"{op}={fn(a,b)}" for each.
"""
# TODO
