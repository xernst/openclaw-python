# Checkpoint — Chapter 11

1. Why is `def f(items=[])` a bug magnet?
2. What does a function return if you omit `return`?
3. What does the leading `*` do in `def f(*, topic, message)`?
4. Where does a docstring go and who reads it?
5. Why do guard clauses produce cleaner code than nested `if`/`else`?
6. Can functions be dict values? Why is that useful?
7. Difference between a pure and side-effecting function.
