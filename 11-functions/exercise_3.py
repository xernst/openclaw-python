"""
Exercise 11.3 — Guard clauses.
Write route(frontmatter) -> str using guard clauses:
  empty dict     -> "inbox"
  status=="archive" -> "archive"
  "project" in tags -> "projects"
  "area" in tags    -> "areas"
  else              -> "resources"
Test with 5 different dicts.
"""
# TODO
