"""
Exercise 11.4 — Safe default.
Write add_item(item, cart=None) that appends item to cart (or new list) and returns the cart.
Call it TWICE without passing cart and confirm the returned lists are independent.
"""
# TODO
