# Checkpoint — Chapter 8

1. Difference between `.sort()` and `sorted()`.
2. What does `b = a` NOT do if `a` is a list?
3. How do you get the last element?
4. Difference between `.remove(x)` and `.pop(i)`?
5. `.append([4,5])` vs `.extend([4,5])` — what's the difference?
6. Output of `[1,2] * 3`?
7. What does `sort(key=len)` do?
