# Checkpoint — Chapter 7

1. When `while` vs `for`?
2. What does `enumerate(items, start=1)` yield?
3. Difference between `break` and `continue`.
4. Write a list comprehension for squares of evens in 0..9.
5. What does `range(10, 0, -1)` produce?
6. When does `for/else`'s `else` NOT run?
7. Does `zip(a, b)` stop at the shortest or longest iterable?
