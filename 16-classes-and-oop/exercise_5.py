"""
Exercise 16.5 — Alternative constructor.
Define Movie(title, year). Add @classmethod from_dict(d) that returns
  Movie(d["title"], d["year"]).
Build one via from_dict({"title":"Inception","year":2010}) and print it.
"""
# TODO
