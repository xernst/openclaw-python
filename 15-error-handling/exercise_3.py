"""
Exercise 15.3 — Custom exception.
Define InvalidPosting(Exception). Write score_posting(title,salary):
  raises InvalidPosting("title must be non-empty") if not title
  raises InvalidPosting("salary must be positive") if salary<=0 or is None
  otherwise returns 42.
Call with ("",100000), ("PM",0), ("AI PM",200000). Print the result or error.
"""
# TODO
