# Checkpoint — Chapter 15

1. Why is bare `except:` dangerous?
2. What does `finally` do? How does `with open(...)` use it?
3. Name three common built-in exceptions.
4. What's EAFP and why is it Pythonic?
5. When do you write a custom exception vs. reuse `ValueError`?
6. What does `raise X from e` preserve?
7. Difference between `raise` and `raise e`?
