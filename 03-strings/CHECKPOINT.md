# Checkpoint — Chapter 3

1. What do `f` and `r` prefixes do on a string?
2. Are strings mutable or immutable?
3. What does `"abc"[::-1]` produce?
4. Write an f-string that formats `0.25` as `25.0%`.
5. Difference between `.split(",")` and `",".join([...])`?
6. Why prefer `"".join(parts)` over `+=` in a loop?
7. What does `f"{x = }"` print when `x = 42`?
