# Checkpoint — Chapter 14

1. Right way to join paths in 2026?
2. Why use a `with` block for files?
3. Difference between `"w"` and `"a"` modes?
4. What does `.rglob("*.md")` do?
5. Why always pass `encoding="utf-8"`?
6. `json.dumps` vs `json.dump` — when would you use which?
7. How to make a folder `a/b/c` safely even if `a/b` exists and `c` doesn't?
