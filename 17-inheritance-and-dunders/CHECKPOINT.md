# Checkpoint — Chapter 17

1. What does `super().__init__(...)` do?
2. When to inherit vs compose?
3. What does `@abstractmethod` enforce?
4. If you define `__eq__`, what else should you usually define?
5. Two dunders that make your object work with `len()` and `for x in obj`.
6. What's the Method Resolution Order (MRO) used for?
7. What does `@total_ordering` give you?
