"""
Exercise 17.3 — ABC.
class Notifier(ABC) with abstract send(text).
Implement EmailNotifier and SlackNotifier (prints are fine).
Loop through a list of notifiers and call send("hello").
"""
# TODO
