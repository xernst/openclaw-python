# Checkpoint — Chapter 6

1. What character ends a line that opens a block?
2. Difference between `==` and `is`?
3. What's `match`'s catch-all pattern?
4. Rewrite `if x > 0: s = "+" else: s = "-"` as a ternary.
5. Why is `if name:` idiomatic for "non-empty string"?
6. What does `x = a or b` assign when `a` is `""`?
7. Python supports chained compares like `0 < x < 100` — is that one operation or two?
