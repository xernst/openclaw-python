# Checkpoint — Chapter 13

1. What makes a folder a package?
2. What does `__name__ == "__main__"` guard against?
3. Three import styles — when do you pick each?
4. What's a relative import (`from .foo import bar`)?
5. Why is `from x import *` frowned upon?
6. How does Python decide where to look for a module?
7. What's a typical cause of a circular import, and one way to fix it?
