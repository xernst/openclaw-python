# Checkpoint — Chapter 12

1. What does LEGB stand for?
2. Type of `*args`? Of `**kwargs`?
3. What does `print(*[1,2,3])` equal?
4. When is a lambda a good idea? When is a `def` better?
5. Why prefer list comprehension over `map(lambda ...)` most of the time?
6. What does `nonlocal` do?
7. Write a one-liner that sorts `[{"age":40},{"age":22}]` by age.
