# Checkpoint — Chapter 2

1. Which PEP dictates naming conventions?
2. Difference between `None`, `False`, and `0`?
3. Why do we write `x is None` instead of `x == None`?
4. Is `True + True == 2` valid Python? Why?
5. Why is `isinstance(x, int)` usually preferred over `type(x) == int`?
6. Name three built-in names you should avoid using as variable names.
7. What does `first, *rest = [1, 2, 3, 4]` assign?
