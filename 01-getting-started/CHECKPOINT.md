# Checkpoint — Chapter 1

1. Which character starts a comment?
2. Name two ways to run a Python file.
3. What does `print("hi" * 3)` output?
4. What does the `end=` keyword on `print()` control?
5. What's the difference between `str()` and `repr()`?
6. How do you quit the REPL?
7. Why does `print(1, 2)` put a space between 1 and 2?
