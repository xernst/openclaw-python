# Checkpoint — Chapter 4

1. Difference between `/` and `//`? Example with negatives.
2. What does `7 % 3` equal? What does `%` mean?
3. Why can `0.1 + 0.2` print `0.30000000000000004`?
4. Which module for money-accurate math?
5. Why prefer `math.isclose(a, b)` over `a == b` for floats?
6. What's `round(2.5)` in Python? Why might it surprise you?
7. What does `2 ** 3 ** 2` equal, and why?
